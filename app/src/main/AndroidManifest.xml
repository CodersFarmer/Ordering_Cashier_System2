<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.HeyGuo.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity">

        </activity>
        <activity android:name=".ui.activity.LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>>
        <activity android:name=".demo.WebViewActivity" />
        <activity android:name=".base.BaseActivity" />
        <activity android:name=".ui.activity.SelectActivity" />
        <activity android:name=".ui.activity.CheckOutActivity" />
        <activity android:name=".ui.activity.RetreatFoodActivity" />
        <activity android:name=".demo.ListViewNestActivity" />
        <activity android:name=".ui.activity.OrderDataActivity" />
    </application>

</manifest>